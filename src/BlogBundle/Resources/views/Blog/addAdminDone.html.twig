{% extends '::layout.html.twig' %}

{% block title %} Confirmation {% endblock %}

{% block corps %}
<section>
	<div class="container" style=" margin-top: 10px;">
		<article>
		{% if afficherFlash %}
		    <div class="alert alert-{{typeFlash}}">
		        <strong>{{flash|e}}</strong>
		    </div>

		    <a href="{{path('blog_homepage')}}"><button type="submit" id="_submit" name="_submit" class="btn btn-default">
							Retour a la page d'accueil
						</button></a>
		{% endif %}
		<article>
	</div>
</section>
{% endblock %}